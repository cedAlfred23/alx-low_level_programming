#include <stdio.h>
#include "main.h"

/**
 * print_last_digit - prints the last digit of a number.
 * @r: the int to check
 * Return: 0 on success
 */
int print_last_digit(int r)
{
	return (r % 10);
}
