#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;
	char hj = " ";

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	putchar(hj);
	return (0);
}
